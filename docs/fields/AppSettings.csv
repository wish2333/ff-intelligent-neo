Field Name,Type,Default,Required,Description,Validation Rules
max_workers,int,2,Yes,Maximum concurrent task workers,1-8 recommended, depends on CPU cores
default_output_dir,str,"",No,Default output directory for processed files,If empty, outputs to source file directory
ffmpeg_path,str,"",No,Custom FFmpeg binary path,If empty, uses auto-downloaded FFmpeg
ffprobe_path,str,"",No,Custom FFprobe binary path,If empty, uses auto-downloaded FFprobe
theme,str,"auto",No,"Theme preference: auto/light/dark","auto = follow system preference; light/dark = manual; persisted to settings.json via save_settings API"
language,str,"auto",No,"Language preference: auto/zh-CN/en","auto = follow system language (Chinese priority); zh-CN/en = manual; persisted to settings.json via save_settings API; used by vue-i18n"
auto_editor_path,str,"",No,Custom auto-editor binary path,"If empty, auto-cut feature disabled; set via Settings page; validated via --version check (>=30.1.0, <31.0.0); persisted to settings.json via save_settings API"
